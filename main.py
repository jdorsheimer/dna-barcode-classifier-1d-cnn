import os
import utilities.data_processing
import utilities.tensor_utils
import utilities.other_utils


# file_path = os.path.join('data','DS-HECALIB_20851.fas')
# df = utilities.data_processing.fasta_to_dataframe(file_path)
#
# stacked = utilities.tensor_utils.stacked_one_hot_1d_tensors(df['Sequence'])


# batch_size = 1
# channels = 1
# sequence_length = stacked.shape[1]
#
# input_tensor = stacked.reshape(batch_size, channels, sequence_length)
# print(input_tensor.shape)
# print(input_tensor)

